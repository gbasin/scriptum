# Relay auth + runtime configuration for local Docker compose.
# Copy to docker/.env and replace placeholder values.

# GitHub OAuth app credentials
# Create at: GitHub Settings -> Developer settings -> OAuth Apps
# Callback URL: http://localhost:8080/v1/auth/oauth/github/callback
GITHUB_CLIENT_ID=replace-with-github-client-id
GITHUB_CLIENT_SECRET=replace-with-github-client-secret

# Relay JWT secret (32+ bytes recommended)
# Generate: openssl rand -base64 32
JWT_SECRET=replace-with-a-strong-jwt-secret

# Relay/Postgres connection for the relay container
DATABASE_URL=postgres://scriptum:scriptum@postgres:5432/scriptum

# Optional overrides
# SCRIPTUM_RELAY_WS_BASE_URL=ws://localhost:8080
# SCRIPTUM_RELAY_SHARE_LINK_BASE_URL=http://localhost:3000/share
