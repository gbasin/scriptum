# Auto detect text files and perform LF normalization
* text=auto

# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads

# Preserve explicit CRLF bytes for golden line-ending fixtures.
tests/golden/cases/line_ending_variation/*.md -text
